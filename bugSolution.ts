function add(a: number, b: number): number {
  return a + b;
}

let result = add(1, 2); // Correct

function addString(a: string, b: string): string{
  return a + b; //String Concatenation
}

let resultString = addString("1","2"); //Correct